# C++ Log Analyzer

A command-line log analysis tool written in C++ that parses structured JSON logs and allows users to load, view, and manage log entries.

The program demonstrates file I/O, JSON parsing, enums, structured data modeling, and basic CLI interaction.

## Build Instructions

Make sure you have g++ and make installed.

Install JSON library (Ubuntu/Pop!_OS)

Execute make

Execute **main** file